{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all AimMod windows",
  "windows": ["overlay", "settings", "logs", "stats"],
  "permissions": [
    "core:default",
    "store:default",
    "shell:default",
    "updater:default",
    "process:default"
  ]
}
